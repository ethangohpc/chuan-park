# Agent portrait

Add the portrait here, for example `ethan-goh.jpg` (720 × 900), then point
`profileImage` in `src/data/agent.ts` at it.

`placeholder-agent.svg` is used until a real portrait is supplied.
